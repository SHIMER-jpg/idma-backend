// const getSizeAndColor = (string) => {
//   const sizeAndColor = string.split("(")[1].split(",");
//   console.log(sizeAndColor);
//   if (sizeAndColor.length > 2) {
//     return "-", "-";
//   }

//   const size = sizeAndColor[0];
//   const color = sizeAndColor[1];
//   return { size, color };
// };

// module.exports = {
//   getSizeAndColor,
// };
